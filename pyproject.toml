[project]
name = "kps-analyst-workbench"
version = "0.1.0"
description = "Codex plugin for research source packets and draft analyst memos."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

